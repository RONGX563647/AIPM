# Supabase (Postgres) connection example for backend
# 1) Go to Supabase Project -> Settings -> Database -> Connection info
# 2) Copy the host and password (do NOT commit secrets to git)

# JDBC URL format for Spring Boot
# Note: Supabase requires SSL; add ?sslmode=require to the JDBC URL
# Example host: find it in Supabase Console -> Settings -> Database -> Connection info
SPRING_DATASOURCE_URL=jdbc:postgresql://<YOUR_DB_HOST>:5432/postgres?sslmode=require
SPRING_DATASOURCE_USERNAME=postgres
SPRING_DATASOURCE_PASSWORD=<YOUR_DB_PASSWORD>
SPRING_DATASOURCE_DRIVER_CLASS_NAME=org.postgresql.Driver

# Optional server overrides
SERVER_PORT=8080
SERVER_SERVLET_CONTEXT_PATH=/api
