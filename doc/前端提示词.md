# AI全自动研发大脑 - Vue3开发详细提示词

## 🎯 **项目初始化与配置**

### **1. 项目创建与环境配置**
```bash
# 1. 创建Vue3项目
npm create vue@latest ai-rnd-brain
# 选择: TypeScript, Vue Router, Pinia, ESLint, Prettier

# 2. 安装核心依赖
npm install element-plus @element-plus/icons-vue echarts vue-echarts
npm install @vueuse/core three @tsparticles/vue3 gsap
npm install axios dayjs lodash-es
npm install -D sass unplugin-auto-import unplugin-vue-components

# 3. 安装开发工具
npm install -D @types/node @types/lodash-es
```

### **2. Vue3项目结构规划**
```typescript
// 项目结构说明
src/
├── assets/               # 静态资源
│   ├── images/          # 图片资源
│   ├── fonts/           # 字体文件
│   └── styles/          # 样式文件
│       ├── variables.scss    # CSS变量定义
│       ├── mixins.scss       # SCSS混合
│       ├── global.scss       # 全局样式
│       └── element-theme.scss # Element Plus主题定制
│
├── components/          # 组件目录
│   ├── layout/         # 布局组件
│   │   ├── SmartSidebar/     # 智能侧边栏
│   │   ├── IntelligentTopbar/ # 智能顶部栏
│   │   ├── BottomDock/       # 底部工作栏
│   │   └── WorkspaceGrid/    # 工作区网格
│   ├── common/         # 通用组件
│   │   ├── GoldenButton/     # 金色按钮
│   │   ├── DataCard/         # 数据卡片
│   │   ├── StatusBadge/      # 状态徽章
│   │   └── LoadingShimmer/   # 加载动画
│   ├── visualization/  # 可视化组件
│   │   ├── TimeRiver/        # 时间河流
│   │   ├── GalaxyMap/        # 宇宙星图
│   │   ├── NeuronNetwork/    # 神经网络
│   │   └── CodeUniverse/     # 代码宇宙
│   └── business/       # 业务组件
│       ├── ProjectCard/      # 项目卡片
│       ├── TaskBoat/         # 任务船只
│       ├── InterfaceNode/    # 接口节点
│       └── DeploymentTimeline/ # 部署时间线
│
├── composables/        # Composition API 函数
│   ├── useSmartSidebar.ts    # 侧边栏逻辑
│   ├── useWorkspaceLayout.ts # 工作区布局
│   ├── useDragAndDrop.ts     # 拖拽功能
│   ├── use3DTransform.ts     # 3D变换
│   ├── useGestureControl.ts  # 手势控制
│   ├── useVoiceCommand.ts    # 语音命令
│   └── useResponsive.ts      # 响应式适配
│
├── stores/            # Pinia状态管理
│   ├── user.store.ts         # 用户状态
│   ├── project.store.ts      # 项目管理
│   ├── task.store.ts         # 任务管理
│   ├── interface.store.ts    # 接口管理
│   ├── codeReview.store.ts   # 代码评审
│   ├── monitoring.store.ts   # 监控状态
│   └── ui.store.ts          # UI状态
│
├── router/            # 路由配置
│   ├── index.ts             # 主路由
│   ├── routes.ts            # 路由定义
│   └── guards.ts            # 路由守卫
│
├── views/             # 页面组件
│   ├── Login.vue           # 登录页
│   ├── Dashboard.vue       # 工作台
│   ├── Projects.vue        # 项目管理
│   ├── Tasks.vue           # 任务管理
│   ├── Interfaces.vue      # 接口中心
│   ├── CodeReview.vue      # 代码评审
│   ├── APITesting.vue      # 接口测试
│   ├── Monitoring.vue      # 监控告警
│   └── Deployments.vue     # 部署记录
│
├── api/              # API接口
│   ├── auth.api.ts        # 认证接口
│   ├── project.api.ts     # 项目接口
│   ├── task.api.ts        # 任务接口
│   ├── interface.api.ts   # 接口管理
│   ├── ai.api.ts          # AI接口
│   └── monitoring.api.ts  # 监控接口
│
├── utils/            # 工具函数
│   ├── constants.ts       # 常量定义
│   ├── helpers.ts         # 辅助函数
│   ├── formatters.ts      # 格式化工具
│   ├── validators.ts      # 验证工具
│   └── animation.ts       # 动画工具
│
├── types/            # TypeScript类型定义
│   ├── index.ts          # 类型导出
│   ├── user.types.ts     # 用户类型
│   ├── project.types.ts  # 项目类型
│   ├── task.types.ts     # 任务类型
│   └── ui.types.ts       # UI类型
│
├── App.vue           # 根组件
└── main.ts          # 入口文件
```

## 🎨 **黑金主题样式配置**

### **1. CSS变量定义 (variables.scss)**
```scss
// 颜色系统 - 黑金主题
:root {
  // 黑色系
  --color-primary-black: #111113;
  --color-secondary-black: #1A1B1E;
  --color-dark-gray: #2C2D31;
  --color-medium-gray: #4A4B4F;
  --color-light-gray: #8E8E93;
  --color-ultra-light-gray: #F2F2F7;
  
  // 金色系
  --color-gold-primary: #FFD700;
  --color-gold-light: #FFED4E;
  --color-gold-dark: #B38B2D;
  --color-gold-darker: #8B6914;
  --color-gold-glass: rgba(255, 215, 0, 0.1);
  
  // 功能色
  --color-success: #34C759;
  --color-warning: #FF9500;
  --color-danger: #FF3B30;
  --color-info: #007AFF;
  --color-link: #0A84FF;
  
  // 渐变
  --gradient-gold: linear-gradient(135deg, var(--color-gold-primary) 0%, var(--color-gold-dark) 100%);
  --gradient-gold-reverse: linear-gradient(135deg, var(--color-gold-dark) 0%, var(--color-gold-primary) 100%);
  --gradient-black: linear-gradient(135deg, var(--color-primary-black) 0%, var(--color-dark-gray) 100%);
  --gradient-glass: linear-gradient(135deg, rgba(255, 215, 0, 0.05) 0%, rgba(255, 215, 0, 0.02) 100%);
  
  // 阴影
  --shadow-sm: 0 1px 3px rgba(0, 0, 0, 0.3);
  --shadow-md: 0 4px 12px rgba(0, 0, 0, 0.4);
  --shadow-lg: 0 8px 24px rgba(0, 0, 0, 0.5);
  --shadow-xl: 0 12px 36px rgba(0, 0, 0, 0.6);
  --shadow-gold: 0 0 20px rgba(255, 215, 0, 0.3);
  
  // 间距系统 (8px基准)
  --spacing-0: 0;
  --spacing-xs: 4px;
  --spacing-sm: 8px;
  --spacing-md: 16px;
  --spacing-lg: 24px;
  --spacing-xl: 32px;
  --spacing-xxl: 48px;
  --spacing-xxxl: 64px;
  
  // 圆角
  --radius-sm: 4px;
  --radius-md: 8px;
  --radius-lg: 12px;
  --radius-xl: 16px;
  --radius-full: 9999px;
  
  // 动画时长
  --duration-fast: 150ms;
  --duration-normal: 300ms;
  --duration-slow: 500ms;
  --duration-slower: 800ms;
  
  // 缓动函数
  --ease-in-out: cubic-bezier(0.4, 0, 0.2, 1);
  --ease-out: cubic-bezier(0, 0, 0.2, 1);
  --ease-in: cubic-bezier(0.4, 0, 1, 1);
  --ease-bounce: cubic-bezier(0.68, -0.55, 0.265, 1.55);
  
  // 玻璃态模糊
  --backdrop-blur-sm: blur(4px);
  --backdrop-blur-md: blur(8px);
  --backdrop-blur-lg: blur(16px);
  --backdrop-blur-xl: blur(24px);
  
  // 边框
  --border-gold-sm: 1px solid rgba(255, 215, 0, 0.3);
  --border-gold-md: 2px solid var(--color-gold-primary);
  --border-gray: 1px solid var(--color-medium-gray);
  --border-gray-light: 1px solid var(--color-light-gray);
}
```

### **2. Element Plus主题定制**
```scss
// element-theme.scss
@forward 'element-plus/theme-chalk/src/common/var.scss' with (
  $colors: (
    'primary': (
      'base': #FFD700,
      'light-3': #FFED4E,
      'dark-2': #B38B2D,
    ),
    'success': (
      'base': #34C759,
    ),
    'warning': (
      'base': #FF9500,
    ),
    'danger': (
      'base': #FF3B30,
    ),
    'error': (
      'base': #FF3B30,
    ),
    'info': (
      'base': #007AFF,
    ),
  ),
  
  $bg-color: (
    'page': #0A0A0B,
    'overlay': rgba(0, 0, 0, 0.8),
  ),
  
  $text-color: (
    'primary': #E5E5E5,
    'regular': #C7C7CC,
    'secondary': #8E8E93,
    'placeholder': #636366,
  ),
  
  $border-color: (
    '': #2C2D31,
    'light': #3A3B3F,
    'lighter': #48494D,
    'extra-light': #56575B,
  ),
  
  $fill-color: (
    '': #1A1B1E,
    'light': #2C2D31,
    'lighter': #3A3B3F,
    'extra-light': #48494D,
    'blank': transparent,
  ),
  
  // 组件特定定制
  $button: (
    'border-radius': var(--radius-md),
  ),
  
  $card: (
    'border-radius': var(--radius-lg),
    'bg-color': var(--color-dark-gray),
    'border-color': var(--border-gold-sm),
  ),
  
  $dialog: (
    'bg-color': var(--color-secondary-black),
    'box-shadow': var(--shadow-lg),
    'border-radius': var(--radius-xl),
  ),
  
  $input: (
    'bg-color': var(--color-dark-gray),
    'border-color': var(--color-medium-gray),
    'hover-border-color': var(--color-gold-primary),
    'focus-border-color': var(--color-gold-primary),
  ),
  
  $table: (
    'border-color': var(--color-medium-gray),
    'bg-color': transparent,
    'header-bg-color': var(--color-secondary-black),
    'row-hover-bg-color': rgba(255, 215, 0, 0.05),
    'current-row-bg-color': rgba(255, 215, 0, 0.1),
  ),
  
  $tag: (
    'border-radius': var(--radius-full),
  ),
  
  $progress: (
    'percentage-text-color': var(--color-gold-primary),
  )
);
```

### **3. 全局样式配置**
```scss
// global.scss
// 1. 重置样式
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

// 2. 基础样式
body {
  font-family: 'Inter', 'PingFang SC', 'Microsoft YaHei', sans-serif;
  font-size: 14px;
  line-height: 1.5;
  color: var(--color-ultra-light-gray);
  background-color: var(--color-primary-black);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

// 3. 滚动条样式
::-webkit-scrollbar {
  width: 8px;
  height: 8px;
}

::-webkit-scrollbar-track {
  background: var(--color-secondary-black);
  border-radius: var(--radius-full);
}

::-webkit-scrollbar-thumb {
  background: var(--color-gold-dark);
  border-radius: var(--radius-full);
  
  &:hover {
    background: var(--color-gold-primary);
  }
}

// 4. 选择文本样式
::selection {
  background-color: var(--color-gold-glass);
  color: var(--color-gold-primary);
}

// 5. 链接样式
a {
  color: var(--color-link);
  text-decoration: none;
  transition: color var(--duration-fast) var(--ease-in-out);
  
  &:hover {
    color: var(--color-gold-primary);
  }
}

// 6. 图片样式
img {
  max-width: 100%;
  height: auto;
}

// 7. 输入框占位符
::placeholder {
  color: var(--color-light-gray);
  opacity: 0.7;
}

// 8. 禁用状态
.disabled {
  opacity: 0.5;
  cursor: not-allowed;
  pointer-events: none;
}

// 9. 工具类
.text-gold {
  color: var(--color-gold-primary);
}

.bg-glass {
  background: var(--gradient-glass);
  backdrop-filter: var(--backdrop-blur-md);
  border: var(--border-gold-sm);
}

.shadow-gold {
  box-shadow: var(--shadow-gold);
}

.gold-border {
  border: var(--border-gold-md);
  border-radius: var(--radius-md);
}

// 10. 动画类
.fade-enter-active,
.fade-leave-active {
  transition: opacity var(--duration-normal) var(--ease-in-out);
}

.fade-enter-from,
.fade-leave-to {
  opacity: 0;
}

.slide-up-enter-active,
.slide-up-leave-active {
  transition: all var(--duration-normal) var(--ease-out);
}

.slide-up-enter-from,
.slide-up-leave-to {
  transform: translateY(20px);
  opacity: 0;
}

.scale-enter-active,
.scale-leave-active {
  transition: all var(--duration-normal) var(--ease-out);
}

.scale-enter-from,
.scale-leave-to {
  transform: scale(0.95);
  opacity: 0;
}
```

## 🔧 **Vue3配置文件**

### **1. Vite配置 (vite.config.ts)**
```typescript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import AutoImport from 'unplugin-auto-import/vite'
import Components from 'unplugin-vue-components/vite'
import { ElementPlusResolver } from 'unplugin-vue-components/resolvers'
import { fileURLToPath, URL } from 'node:url'

export default defineConfig({
  plugins: [
    vue(),
    AutoImport({
      imports: [
        'vue',
        'vue-router',
        'pinia',
        {
          '@vueuse/core': [
            'useMouse',
            'useDebounceFn',
            'useLocalStorage',
            'useElementSize',
            'useDraggable',
            'useGesture'
          ]
        }
      ],
      dts: 'src/auto-imports.d.ts',
      resolvers: [ElementPlusResolver()],
    }),
    Components({
      resolvers: [
        ElementPlusResolver({
          importStyle: 'sass',
        })
      ],
      dts: 'src/components.d.ts',
      dirs: ['src/components'],
    }),
  ],
  
  resolve: {
    alias: {
      '@': fileURLToPath(new URL('./src', import.meta.url))
    }
  },
  
  css: {
    preprocessorOptions: {
      scss: {
        additionalData: `
          @use "@/styles/variables.scss" as *;
          @use "@/styles/mixins.scss" as *;
          @use "@/styles/element-theme.scss" as *;
        `
      }
    }
  },
  
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          'vue-vendor': ['vue', 'vue-router', 'pinia'],
          'element-plus': ['element-plus'],
          'echarts': ['echarts', 'vue-echarts'],
          'threejs': ['three', '@tsparticles/vue3'],
          'animation': ['gsap'],
          'utils': ['axios', 'dayjs', 'lodash-es']
        },
        chunkFileNames: 'assets/js/[name]-[hash].js',
        entryFileNames: 'assets/js/[name]-[hash].js',
        assetFileNames: (assetInfo) => {
          if (/\.(woff2?|eot|ttf|otf)$/.test(assetInfo.name || '')) {
            return 'assets/fonts/[name]-[hash][extname]'
          }
          if (/\.(png|jpe?g|gif|svg|webp|avif)$/.test(assetInfo.name || '')) {
            return 'assets/images/[name]-[hash][extname]'
          }
          return 'assets/[name]-[hash][extname]'
        }
      }
    },
    chunkSizeWarningLimit: 1000,
    sourcemap: false,
    minify: 'terser',
    terserOptions: {
      compress: {
        drop_console: true,
        drop_debugger: true
      }
    }
  },
  
  server: {
    port: 3000,
    host: true,
    open: true,
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true,
        rewrite: (path) => path.replace(/^\/api/, '')
      }
    }
  },
  
  optimizeDeps: {
    include: [
      'vue',
      'vue-router',
      'pinia',
      'element-plus',
      '@element-plus/icons-vue',
      'echarts',
      'vue-echarts'
    ]
  }
})
```

### **2. TypeScript配置 (tsconfig.json)**
```json
{
  "extends": "@vue/tsconfig/tsconfig.dom.json",
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "preserve",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    },
    "types": [
      "vite/client",
      "element-plus/global",
      "node"
    ]
  },
  "include": [
    "src/**/*.ts",
    "src/**/*.d.ts",
    "src/**/*.tsx",
    "src/**/*.vue",
    "auto-imports.d.ts",
    "components.d.ts"
  ],
  "exclude": ["node_modules"]
}
```

### **3. 主入口文件 (main.ts)**
```typescript
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import router from './router'
import ElementPlus from 'element-plus'
import * as ElementPlusIconsVue from '@element-plus/icons-vue'

// 导入样式
import '@/styles/global.scss'
import 'element-plus/dist/index.css'
import 'element-plus/theme-chalk/dark/css-vars.css'

import App from './App.vue'

const app = createApp(App)
const pinia = createPinia()

// 注册Element Plus图标
for (const [key, component] of Object.entries(ElementPlusIconsVue)) {
  app.component(key, component)
}

// 注册自定义图标
import GoldenLogo from '@/components/icons/GoldenLogo.vue'
import GoldenLogoFull from '@/components/icons/GoldenLogoFull.vue'
app.component('GoldenLogo', GoldenLogo)
app.component('GoldenLogoFull', GoldenLogoFull)

app.use(pinia)
app.use(router)
app.use(ElementPlus, {
  size: 'default',
  zIndex: 3000
})

// 全局错误处理
app.config.errorHandler = (err, instance, info) => {
  console.error('Vue Error:', err, instance, info)
}

app.mount('#app')
```

## 🎯 **核心组件开发指南**

### **1. 智能侧边栏组件开发步骤**
```typescript
// SmartSidebar.vue 开发要点

1. **组件结构**:
   - 使用Composition API + TypeScript
   - 分为折叠状态和展开状态
   - 支持hover展开和点击固定

2. **核心功能**:
   - 宽度动画过渡 (30px ↔ 240px)
   - 玻璃态背景效果 (backdrop-filter)
   - 手势支持 (swipe-right/swipe-left)
   - 状态持久化 (localStorage)

3. **交互设计**:
   - 鼠标悬停延迟展开 (300ms)
   - 点击菜单项后自动折叠 (如果未固定)
   - 触摸屏适配 (双指滑动)

4. **样式要点**:
   - 金色边框和装饰线
   - 菜单项选中高亮效果
   - 图标hover发光效果
   - 滚动条定制样式

5. **性能优化**:
   - 使用VueUse的useDebounceFn
   - 虚拟滚动处理大量菜单项
   - 图标懒加载
```

### **2. 数据卡片组件开发步骤**
```typescript
// DataCard.vue 开发要点

1. **3D效果实现**:
   - 使用CSS transform实现3D翻转
   - 鼠标跟随效果 (useMouse + useElementSize)
   - 拖拽功能 (useDraggable)

2. **数据展示**:
   - 正面: 核心数据 + 趋势图表
   - 背面: 详细数据 + 历史记录
   - 边缘: 金色装饰角

3. **交互功能**:
   - 点击翻转查看详情
   - 拖拽改变位置
   - 双击进入专注模式
   - 右键显示上下文菜单

4. **动画效果**:
   - 卡片悬浮轻微浮动
   - 数据更新时的数字滚动
   - 状态变化的颜色渐变

5. **性能考虑**:
   - 图表懒加载
   - 数据变化时的防抖处理
   - 离开视窗时暂停动画
```

### **3. 时间河流组件开发步骤**
```typescript
// TimeRiver.vue 开发要点

1. **可视化实现**:
   - 使用Canvas或SVG绘制河流
   - 任务用船型图标表示
   - 进度用船帆填充高度表示

2. **交互设计**:
   - 拖拽任务在时间轴上移动
   - 点击任务显示详情浮层
   - 滚轮缩放时间范围

3. **动画效果**:
   - 河流流动动画 (CSS animation)
   - 任务移动平滑过渡
   - 状态变化的颜色渐变

4. **性能优化**:
   - 虚拟渲染只显示可见区域
   - 使用Web Workers处理大量数据
   - 防抖处理频繁的视图更新

5. **响应式适配**:
   - 移动端简化视图
   - 触摸屏手势支持
   - 不同屏幕尺寸的布局适配
```

## 🔄 **数据流与状态管理**

### **1. Pinia Store设计**
```typescript
// project.store.ts 示例
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import type { Project, ProjectStatus } from '@/types/project.types'

export const useProjectStore = defineStore('project', () => {
  // 状态
  const projects = ref<Project[]>([])
  const currentProject = ref<Project | null>(null)
  const isLoading = ref(false)
  const error = ref<string | null>(null)

  // Getter
  const activeProjects = computed(() => 
    projects.value.filter(p => p.status === 'active')
  )
  
  const projectCountByStatus = computed(() => {
    const counts = { active: 0, archived: 0, deleted: 0 }
    projects.value.forEach(p => {
      counts[p.status as keyof typeof counts]++
    })
    return counts
  })

  // Actions
  const fetchProjects = async () => {
    isLoading.value = true
    try {
      const data = await projectApi.getProjects()
      projects.value = data
    } catch (err) {
      error.value = err.message
    } finally {
      isLoading.value = false
    }
  }

  const createProject = async (projectData: Partial<Project>) => {
    const newProject = await projectApi.createProject(projectData)
    projects.value.unshift(newProject)
    return newProject
  }

  const updateProject = async (id: string, updates: Partial<Project>) => {
    const updated = await projectApi.updateProject(id, updates)
    const index = projects.value.findIndex(p => p.id === id)
    if (index !== -1) {
      projects.value[index] = { ...projects.value[index], ...updated }
    }
  }

  const deleteProject = async (id: string) => {
    await projectApi.deleteProject(id)
    projects.value = projects.value.filter(p => p.id !== id)
  }

  return {
    // State
    projects,
    currentProject,
    isLoading,
    error,
    
    // Getters
    activeProjects,
    projectCountByStatus,
    
    // Actions
    fetchProjects,
    createProject,
    updateProject,
    deleteProject
  }
})
```

### **2. API层设计**
```typescript
// project.api.ts 示例
import axios from 'axios'
import type { Project, CreateProjectDto, UpdateProjectDto } from '@/types/project.types'

const api = axios.create({
  baseURL: '/api',
  timeout: 10000,
  headers: {
    'Content-Type': 'application/json'
  }
})

// 请求拦截器
api.interceptors.request.use(
  config => {
    const token = localStorage.getItem('token')
    if (token) {
      config.headers.Authorization = `Bearer ${token}`
    }
    return config
  },
  error => Promise.reject(error)
)

// 响应拦截器
api.interceptors.response.use(
  response => response.data,
  error => {
    if (error.response?.status === 401) {
      // 处理未授权
      localStorage.removeItem('token')
      window.location.href = '/login'
    }
    return Promise.reject(error)
  }
)

export const projectApi = {
  // 获取项目列表
  getProjects: (params?: { page?: number; size?: number }) => 
    api.get<Project[]>('/projects', { params }),
  
  // 获取单个项目
  getProject: (id: string) => 
    api.get<Project>(`/projects/${id}`),
  
  // 创建项目
  createProject: (data: CreateProjectDto) => 
    api.post<Project>('/projects', data),
  
  // 更新项目
  updateProject: (id: string, data: UpdateProjectDto) => 
    api.patch<Project>(`/projects/${id}`, data),
  
  // 删除项目
  deleteProject: (id: string) => 
    api.delete(`/projects/${id}`),
  
  // 批量操作
  batchUpdateProjects: (ids: string[], updates: Partial<Project>) => 
    api.post('/projects/batch-update', { ids, updates }),
  
  // 导入项目
  importProjects: (file: File) => {
    const formData = new FormData()
    formData.append('file', file)
    return api.post<Project[]>('/projects/import', formData, {
      headers: {
        'Content-Type': 'multipart/form-data'
      }
    })
  }
}
```

## 🚀 **性能优化策略**

### **1. 组件懒加载**
```typescript
// 路由懒加载配置
const routes = [
  {
    path: '/',
    component: () => import('@/views/Dashboard.vue'),
    meta: { requiresAuth: true }
  },
  {
    path: '/projects',
    component: () => import('@/views/Projects.vue'),
    meta: { requiresAuth: true }
  },
  // ... 其他路由
]

// 组件懒加载
const HeavyChart = defineAsyncComponent(() =>
  import('@/components/visualization/HeavyChart.vue')
)
```

### **2. 虚拟滚动实现**
```typescript
// 使用vue-virtual-scroller
import { RecycleScroller } from 'vue-virtual-scroller'
import 'vue-virtual-scroller/dist/vue-virtual-scroller.css'

// 在大数据列表中使用
<RecycleScroller
  :items="largeDataList"
  :item-size="60"
  key-field="id"
  v-slot="{ item }"
>
  <DataRow :item="item" />
</RecycleScroller>
```

### **3. 图片优化**
```typescript
// 使用v-lazy-image
import VLazyImage from 'v-lazy-image'

// 配置
app.component('VLazyImage', VLazyImage)

// 使用
<v-lazy-image
  :src="imageUrl"
  :src-placeholder="placeholderUrl"
  class="image"
/>
```

## 📱 **响应式适配方案**

### **1. 断点定义**
```typescript
// useResponsive.ts
import { ref, onMounted, onUnmounted } from 'vue'

export const breakpoints = {
  xs: 0,
  sm: 640,
  md: 768,
  lg: 1024,
  xl: 1280,
  xxl: 1536
}

export function useResponsive() {
  const width = ref(window.innerWidth)
  
  const updateWidth = () => {
    width.value = window.innerWidth
  }
  
  onMounted(() => {
    window.addEventListener('resize', updateWidth)
  })
  
  onUnmounted(() => {
    window.removeEventListener('resize', updateWidth)
  })
  
  const isMobile = computed(() => width.value < breakpoints.md)
  const isTablet = computed(() => 
    width.value >= breakpoints.md && width.value < breakpoints.lg
  )
  const isDesktop = computed(() => width.value >= breakpoints.lg)
  
  return {
    width,
    isMobile,
    isTablet,
    isDesktop,
    breakpoints
  }
}
```

### **2. 响应式组件设计**
```vue
<!-- ResponsiveComponent.vue -->
<template>
  <div class="responsive-component">
    <!-- 移动端 -->
    <template v-if="isMobile">
      <MobileView :data="data" />
    </template>
    
    <!-- 平板端 -->
    <template v-else-if="isTablet">
      <TableView :data="data" />
    </template>
    
    <!-- 桌面端 -->
    <template v-else>
      <DesktopView :data="data" />
    </template>
  </div>
</template>

<script setup>
import { useResponsive } from '@/composables/useResponsive'

const { isMobile, isTablet } = useResponsive()
</script>
```

## 🔒 **安全与错误处理**

### **1. 全局错误处理**
```typescript
// errorHandler.js
import { ElNotification } from 'element-plus'

export const errorHandler = {
  // 网络错误
  networkError: (error) => {
    ElNotification.error({
      title: '网络错误',
      message: '请检查网络连接后重试',
      duration: 3000
    })
  },
  
  // 业务错误
  businessError: (error) => {
    ElNotification.warning({
      title: '操作失败',
      message: error.message,
      duration: 3000
    })
  },
  
  // 权限错误
  authError: () => {
    ElNotification.error({
      title: '权限不足',
      message: '您没有权限执行此操作',
      duration: 3000
    })
  },
  
  // 全局错误捕获
  globalError: (error, vm, info) => {
    console.error('Global Error:', error, vm, info)
    // 发送到错误监控平台
    if (process.env.NODE_ENV === 'production') {
      // 调用错误监控API
    }
  }
}
```

## 📦 **构建与部署**

### **1. Docker配置**
```dockerfile
# Dockerfile
FROM node:18-alpine as build-stage
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM nginx:alpine as production-stage
COPY --from=build-stage /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/nginx.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

### **2. Nginx配置**
```nginx
# nginx.conf
server {
    listen 80;
    server_name localhost;
    
    # Gzip压缩
    gzip on;
    gzip_min_length 1k;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml text/javascript application/json application/javascript application/xml+rss;
    
    # 静态资源缓存
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    
    # 主应用
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
    
    # API代理
    location /api/ {
        proxy_pass http://backend:8080/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

## 📋 **开发检查清单**

### **完成每个组件时检查**
- [ ] TypeScript类型定义完整
- [ ] 组件props和emits定义清晰
- [ ] 样式使用CSS变量
- [ ] 响应式适配测试
- [ ] 键盘导航支持
- [ ] 无障碍访问支持
- [ ] 性能优化考虑
- [ ] 错误处理完善
- [ ] 单元测试覆盖
- [ ] 文档注释完整

### **项目完成时检查**
- [ ] 所有路由测试通过
- [ ] 状态管理逻辑正确
- [ ] 接口错误处理完善
- [ ] 生产环境构建无警告
- [ ] 包体积分析优化
- [ ] Lighthouse性能测试
- [ ] 移动端兼容性测试
- [ ] 浏览器兼容性测试
- [ ] 安全扫描通过
- [ ] 部署文档完整

---

**开发建议**：
1. **按模块开发**：先完成基础组件和布局，再开发业务页面
2. **组件复用**：提取通用组件，保持代码一致性
3. **性能优先**：关注首屏加载时间和交互响应
4. **用户体验**：确保动画流畅，交互反馈及时
5. **代码质量**：使用TypeScript严格模式，保持代码规范

这个开发指南涵盖了从项目初始化到部署的完整流程，按照这个指南开发可以确保项目的高质量和可维护性。